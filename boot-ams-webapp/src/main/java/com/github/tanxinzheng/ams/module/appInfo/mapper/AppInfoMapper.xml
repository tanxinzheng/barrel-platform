<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.github.tanxinzheng.ams.module.appInfo.mapper.AppInfoMapper" >

    <insert id="insertSelective" parameterType="com.github.tanxinzheng.ams.module.appInfo.model.AppInfo" useGeneratedKeys="true" keyProperty="id" keyColumn="id" >
        <selectKey resultType="java.lang.String" keyProperty="id" order="BEFORE" >
            SELECT replace(UUID(),'-','')
        </selectKey>
        insert into xmo_app_info
        <trim prefix="(" suffix=")" suffixOverrides="," >
        id,
                <if test="appName != null" >
                    app_name,
                </if>
                <if test="appCode != null" >
                    app_code,
                </if>
                <if test="description != null" >
                    description,
                </if>
                <if test="url != null" >
                    url,
                </if>
                <if test="state != null" >
                    `state`,
                </if>
                <if test="remark != null" >
                    remark,
                </if>
                <if test="devGroup != null" >
                    dev_group,
                </if>
                <if test="testGroup != null" >
                    test_group,
                </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
        #{id, jdbcType=VARCHAR},
                <if test="appName != null" >
                #{appName, jdbcType=VARCHAR},
                </if>
                <if test="appCode != null" >
                #{appCode, jdbcType=VARCHAR},
                </if>
                <if test="description != null" >
                #{description, jdbcType=VARCHAR},
                </if>
                <if test="url != null" >
                #{url, jdbcType=VARCHAR},
                </if>
                <if test="state != null" >
                #{state, jdbcType=INTEGER},
                </if>
                <if test="remark != null" >
                #{remark, jdbcType=VARCHAR},
                </if>
                <if test="devGroup != null" >
                #{devGroup, jdbcType=VARCHAR},
                </if>
                <if test="testGroup != null" >
                #{testGroup, jdbcType=VARCHAR},
                </if>
        </trim>
    </insert>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.String" >
        delete from xmo_app_info where id = #{id}
    </delete>

    <delete id="deletesByPrimaryKey" parameterType="java.util.ArrayList" >
        delete from xmo_app_info where id IN
        <foreach collection="ids" item="item" separator="," open="(" close=")">
        #{item}
        </foreach>
    </delete>

    <update id="updateSelective" parameterType="com.github.tanxinzheng.ams.module.appInfo.model.AppInfo" >
        update xmo_app_info
        <set>
            <if test="id != null" >
            id = #{id, jdbcType=VARCHAR},
            </if>
            <if test="appName != null" >
            app_name = #{appName, jdbcType=VARCHAR},
            </if>
            <if test="appCode != null" >
            app_code = #{appCode, jdbcType=VARCHAR},
            </if>
            <if test="description != null" >
            description = #{description, jdbcType=VARCHAR},
            </if>
            <if test="url != null" >
            url = #{url, jdbcType=VARCHAR},
            </if>
            <if test="state != null" >
            `state` = #{state, jdbcType=INTEGER},
            </if>
            <if test="remark != null" >
            remark = #{remark, jdbcType=VARCHAR},
            </if>
            <if test="devGroup != null" >
            dev_group = #{devGroup, jdbcType=VARCHAR},
            </if>
            <if test="testGroup != null" >
            test_group = #{testGroup, jdbcType=VARCHAR},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateSelectiveByQuery" parameterType="map" >
        update xmo_app_info
        <set >
            <if test="record.id != null" >
            id = #{id, jdbcType=VARCHAR},
            </if>
            <if test="record.appName != null" >
            app_name = #{appName, jdbcType=VARCHAR},
            </if>
            <if test="record.appCode != null" >
            app_code = #{appCode, jdbcType=VARCHAR},
            </if>
            <if test="record.description != null" >
            description = #{description, jdbcType=VARCHAR},
            </if>
            <if test="record.url != null" >
            url = #{url, jdbcType=VARCHAR},
            </if>
            <if test="record.state != null" >
            `state` = #{state, jdbcType=INTEGER},
            </if>
            <if test="record.remark != null" >
            remark = #{remark, jdbcType=VARCHAR},
            </if>
            <if test="record.devGroup != null" >
            dev_group = #{devGroup, jdbcType=VARCHAR},
            </if>
            <if test="record.testGroup != null" >
            test_group = #{testGroup, jdbcType=VARCHAR},
            </if>
        </set>
        <if test="query != null" >
            <include refid="Update_By_Query_Where_Clause"/>
        </if>
    </update>

    <!--    查询消息    -->
    <select id="select"
            resultType="com.github.tanxinzheng.ams.module.appInfo.model.AppInfo"
            parameterType="com.github.tanxinzheng.ams.module.appInfo.model.AppInfoQuery">
        SELECT * FROM xmo_app_info
        <include refid="Update_By_Query_Where_Clause"/>
        ORDER BY id
    </select>

    <select id="selectByPrimaryKey"
            resultType="com.github.tanxinzheng.ams.module.appInfo.model.AppInfo"
            parameterType="java.lang.String">
        SELECT * FROM xmo_app_info WHERE id = #{id}
    </select>

    <select id="selectModelByPrimaryKey"
            resultType="com.github.tanxinzheng.ams.module.appInfo.model.AppInfoModel"
            parameterType="java.lang.String">
        SELECT * FROM xmo_app_info WHERE id = #{id}
    </select>

    <select id="selectModel"
            resultType="com.github.tanxinzheng.ams.module.appInfo.model.AppInfoModel"
            parameterType="com.github.tanxinzheng.ams.module.appInfo.model.AppInfoQuery">
        SELECT * FROM xmo_app_info
        <include refid="Update_By_Query_Where_Clause"/>
        ORDER BY id
    </select>

    <sql id="Update_By_Query_Where_Clause">
        <where>
            <if test="keyword">
                AND id LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="id">
                AND id = #{id}
            </if>
            <if test="ids">
                AND id IN
                <foreach collection="ids" item="item" separator="," open="(" close=")">
                #{item}
                </foreach>
            </if>
            <if test="excludeIds">
                AND id NOT IN
                <foreach collection="excludeIds" item="item" separator="," open="(" close=")">
                #{item}
                </foreach>
            </if>
        </where>
    </sql>

</mapper>